@model VetManage.Web.Models.Account.RecoverPasswordViewModel

@{
    ViewData["Title"] = "Recover Password";
}
<div class="content-container d-flex justify-content-center align-items-center pt-5">
    <div class="form-container mt-5 pt-5">
        <div>
            <p>Please enter your email.</p>
        </div>
        <form method="post" style="width: 350px">
            <div asp-validation-summary="ModelOnly"></div>
            <div class="mb-3">
                <label asp-for="Email">Email</label>
                <input asp-for="Email" class="form-control"/>
                <span asp-validation-for="Email" class="text-warning"></span>
            </div>
            <div class="mb-3">
                <div class="d-flex justify-content-end">
                    <button asp-action="RecoverPassword" asp-controller="Account" type="submit" class="btn btn-success text-light">Recover</button>
                </div>
            </div>
            @*<div class="mb-3">
                <button type="submit" class="btn btn-success">Login</button>
            </div>*@
        </form>
    </div>       
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

                    